extends ../layout/main

block variables
  - var title = 'TEST PAGE';
  - var description = 'TEST PAGE';
  - var pageClass = 'index-page';

block content
  section
    .container
      include ../includes/modules/modal-1


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }
